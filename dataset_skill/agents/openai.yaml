interface:
  display_name: "RAGFlow Dataset, Retrieval and Chat"
  short_description: "Manage datasets, docs, parsing, retrieval, chat assistants, and conversations"
  default_prompt: "Use $ragflow-dataset-ingest to manage RAGFlow datasets, documents, start or stop parsing, check parser status, retrieve relevant chunks, manage chat assistants, send chat messages, manage sessions, and diagnose connectivity. For delete or stop actions, always list items first and require explicit user confirmation before executing."
